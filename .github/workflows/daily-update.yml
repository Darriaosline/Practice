name: Daily Repository Update

permissions:
  contents: write

on:
  schedule:
    - cron: '35 14 * * *'   # fixed daily time
  workflow_dispatch:

jobs:
  update-repo:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Setup Git
        run: |
          git config user.name "DevSync Bot"
          git config user.email "22f1000310@ds.study.iitm.ac.in"

      - name: Daily Commit Step - 22f1000310@ds.study.iitm.ac.in
        run: |
          echo "Run ID: $RANDOM $(date)" >> daily_log.txt
          git add daily_log.txt
          git commit -m "Daily automated update $(date)"
          git push origin HEAD:main
